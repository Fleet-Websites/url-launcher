# Drops

Cheap URL feed. One file **per topic per day**: `YYYY-MM-DD_drop_<slug>.md`.
No topic → `YYYY-MM-DD_drop_untopiced.md`.

One `https://` per line, optional blurb after it. Cursor seats append via MCP
`add_drop`. See `_Governance/Ingest-Convention.md`.
